<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Animated Responsive Login Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes slideIn {
      from { transform: translateX(-100%); }
      to { transform: translateX(0); }
    }
    .animate-fadeIn {
      animation: fadeIn 0.5s ease-out;
    }
    .animate-slideIn {
      animation: slideIn 0.5s ease-out;
    }
  </style>
</head>
<body class="bg-gray-100">
  <div class="flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-md rounded-lg flex max-w-4xl w-full mx-auto overflow-hidden">
      <!-- Left Section: Welcome Back -->
      <div class="w-1/2 bg-teal-500 text-white p-8 hidden md:flex flex-col justify-center animate-slideIn">
        <h1 class="text-4xl font-bold mb-4">Welcome Back!</h1>
        <p class="text-lg mb-6">To keep connected with us, please login with your personal info.</p>
        <a href="/user/signup" class="bg-white text-teal-500 font-bold py-2 px-4 rounded-full text-center inline-block hover:bg-gray-100 transition duration-300">Sign Up</a>
      </div>
      
      <!-- Right Section: Login -->
      <div class="w-full md:w-1/2 bg-white p-8 animate-fadeIn">
        <h2 class="text-2xl font-bold text-teal-500 mb-6">Login</h2>
        
        <form action="/user/login" method="POST">
          <div class="mb-4">
            <label for="email" class="block text-gray-700 text-sm font-bold mb-2">Email</label>
            <input type="email" id="email" name="email" placeholder="Enter your email" class="w-full px-4 py-2 bg-gray-200 rounded-md border focus:outline-none focus:ring-2 focus:ring-teal-500 transition duration-300" required>
          </div>
          
          <div class="mb-6">
            <label for="password" class="block text-gray-700 text-sm font-bold mb-2">Password</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" class="w-full px-4 py-2 bg-gray-200 rounded-md border focus:outline-none focus:ring-2 focus:ring-teal-500 transition duration-300" required>
          </div>
          
          <button type="submit" class="w-full bg-teal-500 text-white font-bold py-2 px-4 rounded-full focus:outline-none hover:bg-teal-600 transition duration-300">Login</button>
        </form>

        <div class="mt-4 text-center">
          <p class="text-sm text-gray-600">Don't have an account?</p>
          <a href="/user/registerPage" class="text-teal-500 font-bold hover:underline">Sign Up</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
